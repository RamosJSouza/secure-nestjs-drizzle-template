# Copy to .env for docker-compose
# cp .env.docker.example .env

NODE_ENV=production
PORT=3000

DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=admin_limify

REDIS_HOST=redis
REDIS_PORT=6379

# Required: generate RSA keys for JWT
PRIVATE_KEY=
PUBLIC_KEY=
